---
title: CSS Exercises
date: 2020-04-25
published: false
---

import { Box, Flex, Button, Grid, Radio, Label } from "@theme-ui/components"
import CodeBlock from "../../../src/components/code-block"

export const Overlay = () => {
  const [state, setState] = React.useState(false)
  const [isSolutionVisible, setSolutionVisible] = React.useState(false)
  const styles = { position: state ? "relative" : "static" }
  return (
    <>
      Center text inside the image so that it looks like this:
      <CodeBlock
        className="language-js"
        live
        hideEditor
        // sx={{ transform: "scale(0.78)" }}
      >
        {`
<div style={{width: 300, height: 300, position: 'relative'}}>
  <img
    style={{position: 'absolute'}}
    src="https://picsum.photos/id/10/300?blur=2&grayscale"
  />
  {/* this div is centered inside the image */}
  <div
    style={{
      position: 'absolute',
      width: '100%',
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }}
  >
    {'Center me inside the image'}
  </div>
</div>
`}
      </CodeBlock>
      <br />
      <h3>Exercise:</h3>
      <CodeBlock className="language-js" live>
        {`
<div style={{width: 300, height: 300}}>
  <img 
    style={{}}
    src="https://picsum.photos/id/10/300?blur=2&grayscale"
  />
  {/* center this div on top of the image */}
  <div style={{}}>
    {'Center me inside the image'}
  </div>
</div>
      `}
      </CodeBlock>
      <Button my={2} onClick={() => setSolutionVisible(!isSolutionVisible)}>
        {isSolutionVisible ? "Hide" : "Show me"} the solution
      </Button>
      {isSolutionVisible && (
        <>
          <br />
          <h3>Solution:</h3>
          <CodeBlock className="language-js" live disabled>
            {`
<div style={{width: 300, height: 300, position: 'relative'}}>
  <img
    style={{position: 'absolute'}}
    src="https://picsum.photos/id/10/300?blur=2&grayscale"
  />
  <div
    style={{
      position: 'absolute',
      width: '100%',
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }}
  >
    {'Center me inside the image'}
  </div>
</div>
      `}
          </CodeBlock>
          <form
            name="css-overlay"
            method="post"
            data-netlify="true"
            data-netlify-honeypot="bot-field"
          >
            <input type="hidden" name="form-name" value="css-overlay" />
            <fieldset
              style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr" }}
            >
              <legend>My solution was:</legend>
              <Label sx={{ alignItems: "center", svg: { mr: "4px" } }}>
                <Radio name="My solution" value="wrong" />
                Wrong
              </Label>
              <Label sx={{ alignItems: "center", svg: { mr: "4px" } }}>
                <Radio name="My solution" value="close" />
                Close
              </Label>
              <Label sx={{ alignItems: "center", svg: { mr: "4px" } }}>
                <Radio name="My solution" value="correct" />
                Correct
              </Label>
            </fieldset>
            <Button
              variant="secondary"
              type="submit"
              sx={{ py: 1, px: 2, cursor: "pointer" }}
            >
              Submit
            </Button>
          </form>
        </>
      )}
    </>
  )
}

## Overlay

<Overlay />
