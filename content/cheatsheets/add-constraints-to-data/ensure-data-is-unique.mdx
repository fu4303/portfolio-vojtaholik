---
title: Ensure Data is unique [`unique`](https://www.w3schools.com/sql/sql_unique.asp)
---

Ensure data stored in a column is unique with the  unique  keyword:

```sql
create table Users (  
  first_name text,
  user_handle uuid not null unique,
  constraint PK_users primary key (user_handle)
);

```
