import styled from "styled-components"
import {
  color,
  flexbox,
  space,
  layout,
  typography,
  border,
} from "styled-system"

const Box = styled("div")(color, flexbox, space, layout, typography, border)

export default Box
